{
  "name": "skill-icons-generator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node generate-icons.mjs --out-dir dist/icons --minify",
    "preview": "vite preview",
    "generate": "node generate-icons.mjs --out-dir generated-icons",
    "sync": "python sync_icons.py"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
